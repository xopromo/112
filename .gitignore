__pycache__/
*.py[cod]
*.pyo
.env
*.db
*.log
vk_sales/config.json

# Testing & Regression Logs (accumulated over time, not committed)
.claude/logs/
.claude/regression-report.json
.regression-results

# NOTE: USE_Optimizer_v6_built.html MUST remain in git for GitHub Pages!
# Until CI/CD is setup to regenerate it, keep it committed.
# TODO: Setup GitHub Actions workflow to auto-rebuild on source changes
# (Do NOT add to .gitignore until CI/CD is ready)

